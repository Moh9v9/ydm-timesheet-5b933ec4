
import Attendance from "@/pages/attendance/Attendance";

const DailyAttendance = () => {
  // Attendance component manages its own logic and context
  return (
    <div>
      <Attendance />
    </div>
  );
};

export default DailyAttendance;
